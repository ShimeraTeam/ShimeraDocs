name: Documentation

permissions:
  contents: write

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - fix/70-fix-github-pages

jobs:
  deploy:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install
        working-directory: ./userdoc

      - name: Build VitePress site
        run: npm run build
        working-directory: ./userdoc

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./userdoc/docs/.vitepress/dist
          publish_branch: gh-pages